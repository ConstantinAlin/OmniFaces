The <o:messages> is a component that extends the standard <h:messages> with the following new features: multiple for components, displaying single message, HTML escaping, iteration markup control ...
http://showcase.omnifaces.org/components/messages